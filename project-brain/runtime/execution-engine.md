# System Runtime - Execution Engine

## Guidelines
1. Execute code patches targeting only isolated modules.
2. Respect CSS variables inside `base.css` and routing boundaries.
3. Once completed, pass to Static Verification (`npm run build`).
